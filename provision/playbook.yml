---
- name: "Provision Vagrant"
  hosts: default
  become: true
  remote_user: vagrant
  roles:
    - repo_install
    - common
    - mysql
    - redis
    - php7
    - composer
    - bash
    - java
